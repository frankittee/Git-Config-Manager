minimum_prek_version = "0.4.0"
default_install_hook_types = ["commit-msg"]

[[repos]]
repo = "local"

[[repos.hooks]]
id = "conventional-commit"
name = "conventional commit message"
entry = "mise x -- cog verify --file"
language = "system"
stages = ["commit-msg"]
always_run = true
